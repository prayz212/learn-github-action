name: 'Auto Assign Review To PR'
on:
  pull_request:
    types: [opened, reopened]
    branches:
      - 'github-action-test/**'

jobs:
  add-reviews:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v1.2.5
